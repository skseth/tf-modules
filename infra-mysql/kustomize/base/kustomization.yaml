apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Points to the directories containing the child kustomizations
resources:
  - configmap.yaml
  - service.yaml
  - service-ext.yaml
  - statefulset.yaml

# Transformations applied to ALL children
labels:
- pairs:
    product: infra
    env: base

