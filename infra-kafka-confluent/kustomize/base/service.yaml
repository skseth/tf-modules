# kafka-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: kafka-service
spec:
  selector:
    app: kafka
  ports:
    - name: external
      protocol: TCP
      port: 9094
      targetPort: 9094
  clusterIP: None # Headless service
  